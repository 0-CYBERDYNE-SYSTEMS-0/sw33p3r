# Build artifacts
dist/
build/
*.fap
*.elf
*.map
.ufbt_state/

# Python tooling
__pycache__/
*.pyc

# Editor
.vscode/
.DS_Store

# Diagnostic scratch scripts (kept in history, not tracked going forward)
diag_*.py
bisect_ladder.py

# Internal AI-workflow evidence, graphs, journals — local only, never publish.
# (.omo/evidence holds unredacted original captures; redacted copies live in docs/field_guide_shots)
.omo/
.codex/
.zcode/
.debug-journal.md

# Promo material ships as GitHub Release assets, not in-repo
promo_video/

# Local backup bundles
*.bundle
