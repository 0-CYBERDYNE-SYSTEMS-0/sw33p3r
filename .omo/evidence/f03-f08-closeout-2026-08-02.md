# F03-F08 host closeout receipt — 2026-08-02

Scope: operator controls, Wi-Fi/BLE browsing, GPS pages/source, single-owner
recording, numbered artifacts, privacy, reports, and operator documentation.

## Commands

```text
git diff --check
PYTHONDONTWRITEBYTECODE=1 python3 _verify_api.py
./init.sh
```

## Result

- `git diff --check`: PASS.
- API surface: PASS, 59/59 call-like symbols exported by installed API 87.1.
- Host NMEA, input, RF/TX, wireless, GPS, recorder, report, and scan helpers:
  PASS with zero failures.
- uFBT APPCHK: PASS, Target 7, API 87.1.
- `dist/room_sweep.fap`: 72,828 bytes.
- SHA-256: `3613578c1cd37f06222c62f08712113429ddb93f10578c14b86d8f4b5bf96fc6`.

## Correctness closeout

- Table-full Wi-Fi/BLE observations use an invalid-index sentinel and are
  reported as drops; they cannot reuse a prior row as new evidence.
- MAC-less/name-less observations are shown and persisted as an unidentified
  observation group, not an identified device count.
- Worker/ISR producers never call storage APIs; the main loop is the only
  session serializer and drains at most eight queued events per pass.
- Logging enable/privacy changes are serialized with the record queue.
- CSV record/byte counters advance only after a successful storage write.
- UART, GPS byte, raw-ring, and table-full losses are explicit drop events and
  make the report incomplete.
- Exact GPS coordinates are a separate opt-in; raw UART snapshot privacy is
  warned explicitly.
- Existing fixed nested logs are left untouched. New numbered files use
  `/ext/apps_data/room_sweep/` and create-new semantics.
- Passive wireless evidence, RSSI, and bounded frequency-only TX limitations
  are stated in the UI, report, README, and operator guide.

Connected-device interaction and RF transmission are not claimed by this host
receipt.
